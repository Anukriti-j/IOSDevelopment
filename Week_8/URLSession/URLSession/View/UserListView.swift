import SwiftUI

struct UserListView: View {
    @StateObject var userViewModel: UserViewModel = UserViewModel()
    
    var body: some View {
        VStack {
            List {
                
            }
        }
    }
}

#Preview {
    UserListView()
}
